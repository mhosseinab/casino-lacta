// Placeholder shell for the scaffold step (S1). Presentation only — no outcome,
// RNG, payout, or balance logic ever lives in the client (see CLAUDE.md iron rules).
// The real shell, lobby, transport seam, and game views arrive in S2+.
export function App() {
  return (
    <main>
      <h1>casino-lacta</h1>
      <p>
        Play-money casino client — scaffold ready. Not real money · No prizes.
      </p>
    </main>
  );
}
