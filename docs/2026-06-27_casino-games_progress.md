---
type: progress
project: casino-games
tags: [casino, migration-orchestration, progress-tracker, dev]
status: NOT STARTED; gaps-patched 2026-06-27
slug: casino-games
date: 2026-06-27
runs: 2026-06-27_casino-games_implementation-steps.md
---

# Casino Games — Orchestration Progress

**Durable state for the orchestrator.** On start/resume, READ this file to know where you are.
Source script: `2026-06-27_casino-games_implementation-steps.md` (S1..S42).
Repo root: `casino-lacta/` (already exists, git-init'd on branch `master`, 0 commits) · Worktree: _TBD (created at SETUP)_ · Integration branch: `casino-games/main` (created off the first commit at S1) · **Integration tip: 884cf62 (S1–S9 merged — P1 spine complete; ⛔ at S9 REVIEW GATE)**
Review routing (BY AREA → the named `.claude/agents`, falling back to `/code-review`):
| Area / step set | Reviewer |
|---|---|
| `engine/**` | `engine-purity-reviewer` |
| `app/wallet`, `app/games/bet_loop`, `app/auth`, `app/economy`, `app/poker` lobby/rake (S3, S6, S8, S33, S36, S37) | `ledger-security-reviewer` |
| `engine/rng`, `engine/fairness`, `verifier/`, `/fairness`, Originals parity tests (S4, S7) | `fairness-rng-reviewer` |
| `app/ws/crash`, `app/ws/poker`, `app/poker/*` realtime (S18–S20, S32–S35) | `realtime-integrity-reviewer` |
| `client/**` | `frontend-renderer-reviewer` |
| everything else | `/code-review` |
| S41 deploy/secrets | `/security-review` |

Security-grade step set (security-grade review required): **S3, S4, S6, S7, S8, S18, S19, S20, S32, S33, S34, S35, S41**. Fallback when a named reviewer is absent: a fresh reviewer subagent + the checklist in the orchestrator REVIEW step.

## Rules in force
- Respect the dependency graph: start a step only when prerequisites are `passed`; run independent steps (disjoint files, no edge) in parallel; never let two steps edit the same file concurrently.
- Honor CARRY-FORWARDs: `engine/src/engine/games/_curve.py` (S10 → reused by S18) is a HARD DAG edge — S18 cannot start until S10 is `passed`. `engine/src/engine/cards/evaluator.py` (S28 5-card core → extended to best-5-of-7 by S29) is a SOFT, shared-file constraint: if both S28 and S29 are in scope, SERIALIZE them (never run concurrently — same file); whichever runs first creates the evaluator, the other extends it (never duplicate).
- Cap 3 review cycles/step → else mark `blocked`, stop, surface a summary.
- Never weaken a project rule or RTP tolerance to pass a check — escalate.
- **Inline gates — require explicit "go":** S9 = REVIEW GATE (human review of the Dice slice before scaling); S40 = HUMAN SIGN-OFF (load test); S41 = DESTRUCTIVE/PAID (deploy).
- Never auto-run paid/live operations (deploy S41, any production action, secret handling).

## ⚠️ Deferred verification gate (needs live infra / paid — run with authorization)
- **S40:** load test (Crash N-client + busy poker table) under `docker compose`; assert ledger reconciles to the cent + no double-credit + p95 latency. Proves it WORKS, not just compiles.
- **S41:** deploy to Fly/Render + managed Postgres/Redis + static client; live `/health`, a real play-money bet, public verifier. Paid/live — human-authorized.
- Note: S3 concurrency, S19/S20 Crash, and S32/S34 poker tests need Postgres+Redis via `docker compose up` locally — acceptable inside the loop if compose is available; otherwise defer to the S40 window.

## Status table

| Step | Title | Status | Attempts | Branch | Commit SHA | Note |
|------|-------|--------|----------|--------|-----------|------|
| S1 | Scaffold repo + tooling + CI + health | passed | 2 | casino-games/main | 7207cac | root commit; rtp-gate placeholder tolerates pytest exit-5 until S7; lefthook=pnpm devDep; uv picked CPython 3.14 |
| S2 | Data model + Alembic migrations | passed | 1 | casino-games/S2 | 2500fb8 | merged→a539a79; up/down clean; SYSTEM+house GOLD wallet seeded; SEAM: GameConfig('originals.dice',v1) pre-seeded → S9 consumes it (no re-seed). Advisory: edge/enabled lack DB server_default |
| S3 | Double-entry ledger service | passed | 2 | casino-games/S3 | 479f3de | merged→6fa6c5d; idempotency=UNIQUE(idempotency_key) player-row + FOR UPDATE no-overspend; +migration a1b2c3. Cycle2 added concurrent-same-key fence test (proven RED w/o constraint) + multi-wallet conservation |
| S4 | RNG + provably-fair module (pure) | passed | 1 | casino-games/S4 | d1ad67a | merged→a539a79; HMAC msg `{cs}:{nonce}:{cursor}`, 4B→uint32/2**32 ∈[0,1); commit/verify reuse stream; matches spec §2.3 |
| S5 | Engine contracts + money + registry | passed | 1 | casino-games/S5 | 5ac1e2b | merged→a539a79; apply_multiplier=floor(stake*mult)→int (house margin), cap=min after rounding; Protocols per build-plan §3; registry empty. Note: math.floor float-trap is spec-mandated+house-favorable, OK |
| S6 | Shared bet loop + game API + RG hook | passed | 1 | casino-games/S6 | 55add13 | merged→20eeb4d; betId saga: validate→idempotent-replay→RG→single debit(WAGER)→nonce+pure outcome+apply_multiplier/cap+Round/Bet/Audit in 1 txn→single credit(WIN); +migration b2c3d4e5f6a7. ledger-security PASS, all findings advisory & deferred → see S7/S8/S38 notes. Full suite 35 green |
| S7 | Verifier + RTP harness + CI gate | passed | 1 | casino-games/S7 | dc2717f | merged→b8a0fd0; verifier reuses engine VERBATIM (server==verifier; reproduce + reproduce_round); /fairness reveals seed only post-rotation; RTP harness CLT half-width tol (z·√(Var/n)), uncapped, rtp_heavy marker + rtp-gate job; proven @0.99 w/ RED-proof. +commit 0f7316f fixes PRE-EXISTING CI defect (S3): python job had NO postgres service→DB tests would error; added postgres:16 svc + alembic upgrade head before pytest, proven migrate-from-empty=45 green. fairness-rng PASS. ADVISORY (deferred): verifier/web JS float-primitive has no auto JS≡engine parity test (add when JS test stage exists); ServerSeed.seed_encrypted is plaintext (encryption-at-rest later seam). NOTE: S6 carry-in (a) does NOT apply — /fairness reads serverSeedId from audit payload, not seed selection |
| S8 | Auth (play-money sessions) | passed | 1 | casino-games/S8 | 8178437 | merged→9c6e61e; guest User+GOLD/PLAY Wallet + JWT access/refresh (HS256 alg-pinned, type-separated, JWT_SECRET from env); current-user dep 401/200. Welcome grant = SINGLE-OWNER seam economy/faucets.py (welcome_grant, key hash(uid,'welcome'), WELCOME_GRANT_MINOR=1e6) via Ledger.grant only — idempotent, no double-grant. +pyjwt; root ruff B008 extend-immutable-calls. ledger-security PASS. ADVISORY: token-type test asymmetric (1 direction); guest committed before JWT_SECRET read (fail-loud OK). CARRY-FWD: S36 replaces faucet policy w/o auth change. NOTE: ran PARALLEL with S9 (disjoint). S6 carry-in (/state query-string uid) NOT yet fixed — still open for a later auth-integration pass |
| S9 | Dice (vertical slice) | passed | 2 | casino-games/S9 | 84574ae | merged→884cf62; ⛔ REVIEW GATE — STOPPED for human go. originals.dice §A.1 (roll=floor(f*1e4)/100, UNDER/OVER, mult=(1-edge)/p, edge from cfg, pure); registered only — bet loop/router/main.py UNCHANGED (OCP proven). engine-purity PASS (gate-grade); RTP gate non-vacuous (edge=0.05→raises). Cycle2 aligned RTP marking to rtp_heavy convention (fast 1e6 {50,98} PR + heavy 1e7 {2,50,98} in rtp-gate job). CARRY-FWD (NOT engine): per-game target input-range fence belongs at app/bet-loop input boundary — must land before/with S10-S17 |
| S9.5 | Bet-boundary hardening (input fence + auth) | passed | 1 | casino-games/S9_5 | 4146303 | merged→8f0c025; GATE-INSERTED (human-approved). (a) validate_input(input,cfg) on InstantGame protocol — engine-owned InvalidBetInput, pure, raises on out-of-range; bet loop enforces BEFORE nonce/debit/play (zero ledger movement on reject)→422; dice §A.1 band p∈[0.0001,0.98] fully guards play(). (b) S8 get_current_user enforced on /games router — user_id REMOVED from BetRequest + /state query, identity from JWT only (forged body uid ignored, no-token→401). Import cycle (api.games↔auth) fixed via deferred imports. ledger-security PASS. ADVISORY (deferred): validate-before-replay ordering; relocate _runtime to neutral module. Merged-tree clean-DB verify: 80 passed/4 deselected. ALL ORIGINALS S10–S17 INHERIT this validate_input seam |
| S10 | Limbo (+ _curve.py) | passed | 1 | casino-games/S10 | c9b45ea | merged→dcba8ce; limbo §A.1/§A.4: 1 rng.next()→f, X=crash_point(f,edge), win iff X≥target, mult=target; validate_input rejects missing/non-finite/target<1.01. CF `engine/games/_curve.py` crash_point=max(1.00,floor((1-edge)/(1-f)*100)/100) → reused VERBATIM by S18. engine-purity PASS. Cycle-2 (ff9c5bc, comment-only): corrected _curve.py comment for S18 — **continuous instant-bust rate P(f<edge)=edge (=spec §A.4 ≈edge); floored mass P(X==1.00)=1-(1-edge)/1.01≈2·edge**. S18 must test bust-rate via P(f<edge), NEVER P(X==1.00). SEED VALUES for consolidated migration: GameConfig edge=0.01 params={}; GameLimit GOLD minBet=1 maxBet=100_000_000 maxWin=1_000_000_000 maxMult=1_000_000. Verify: 31 passed/8 deselected, ruff/mypy(33)/lint-imports(2 kept)/purity-empty |
| S11 | Pocket Dice | passed | 1 | casino-games/S11 | 8a25f02 | merged→f8d4184; §A.2 two dice floor(f*6)+1 sum 2-12, OVER/UNDER, mult=(1-edge)/p with p summed STRUCTURALLY from 2d6 pmf (no paytable→RTP=1-edge every target); fence rejects missing/non-finite/FRACTIONAL/out-of-band + OVER12/UNDER2. engine-purity PASS. SEED: edge=0.01; GameLimit GOLD minBet=1 maxBet=100_000_000 maxWin=1_000_000_000 maxMult≥36 (top 35.64× at p=1/36). Verify: 43 passed/10 desel + heavy 1e7 converges; ruff/mypy/lint-imports/purity-empty |
| S12 | Mines (stateful) | passed | 1 | casino-games/S12 | 33fd1c3 | merged→23b00fe. **FIRST STATEFUL GAME — established the server-held-state saga reused by S14/S25/Blackjack.** engine-purity PASS + ledger-security PASS (both: redaction + FOR-UPDATE lock EMPIRICALLY verified; reconciliation Σ=0 over 840 entries; instant-path regression clean 116 passed). §A.6: mines.py init=sampleWithoutReplacement(25,M) committed at round start (reuses sampling.py), step reveal/cashout, fairMult=C(25,k)/C(25-M,k), payout=(1-edge)·fairMult, EV=1-edge EXACT every (M,k). **SAGA PATTERN (S14 MUST REUSE, don't re-build):** (1) opaque-state seam — app NEVER reads game-specific state keys, drives credit/status off generic Outcome.detail["status"]+multiplier; (2) NEW server-only `game_rounds.server_state` JSONB column holds full opaque state incl. hidden layout — /state returns fixed SAFE key set, NEVER server_state (layout never leaks while ACTIVE; disclosed only in TERMINAL outcome); (3) StatefulGame.validate_input ADDED to protocol (engine/types.py, mirrors InstantGame, no no-op default) — fence runs BEFORE guard/debit; (4) action serialization = SELECT...FOR UPDATE on GameRound row per /action (distinct from assert_no_active_round which only blocks a 2nd round); (5) single-debit hash(roundId,"WAGER") at open / single-credit hash(roundId,"WIN") at cashout, reveals move no money, reveal idempotency intrinsic (no per-action key). Migration c3d4e5f6a7b8 (down_rev=b2c3d4e5f6a7, linear, reversible both ways). SEED: edge=0.01 params={} (M is per-bet input); GameLimit GOLD min_bet=1 max_bet=100_000_000 max_win=1_000_000_000 max_multiplier=5148297 (informational — real cap is max_win via engine.money.cap after floor). ADVISORIES for S14+: (a) one-active-round guard is advisory/TOCTOU (UX not money-safety; reconciliation intact — a hard guard needs partial unique idx on (user_id,game_id) WHERE status='ACTIVE'); (b) stateful cashout self-heal needs a re-sent cashout (/bet-replay + /state don't re-issue a stranded credit, unlike instant _finalize_existing); (c) NO settlement AuditEvent on stateful cashout/loss (only ROUND_OPENED) — thinner audit trail than instant BET_SETTLED, consider adding. Verify: tests 83 mines (12 saga DB + 71 pure) + full suite 384 passed/42 desel; merged-tree purity-empty/ruff/mypy(39)/lint-imports(2)/registry-8/single-head |
| S13 | Plinko (tuned table) | passed | 1 | casino-games/S13 | 7c02e0b | merged→ea7690d (registry conflict resolved, all entries kept; worker stalled pre-commit→resumed). §A.5 rightBounces=Σ(rng.next()<0.5); boards rows{8,12,16}×risk{LOW,MED,HIGH}=9, analytic RTP 0.99 max-dev 1e-6 post-round (reviewer hand-verified R8/LOW=25344/25600); single ball/play (multi-ball=app). registry.py carries _PLINKO_TABLES const + entry. SEED: edge=0.01 params.tables (JSON-string row keys); top mult 13.99 → maxMultiplier≥14, minBet=1 maxBet=100_000 maxWin=1_400_000. engine-purity PASS. Verify: 75 passed/12 desel + heavy 1e7 passed |
| S14 | HiLo (stateful) | passed | 2 | casino-games/S14 | 16a0739 | merged→f50a67f. **SECOND stateful game — EXTENDED the S12 saga for PROGRESSIVE-DRAW games (pattern S25 Blackjack/Baccarat inherit).** engine-purity PASS + ledger-security PASS (both: no-seed-in-state EMPIRICALLY inspected on a live round = server_state keys exactly {cursor, game}; redaction empirically proven via leaky-public_view test; reconciliation Σ=0 over 752 txns; Mines regression clean). §A.7: hilo.py cards=floor(next()*52)//4+1 (uniform 1..13), p(H)=(14-r)/13 p(L)=r/13, stepMult=(1-edge)/p(side), cumulative=Π, tie WINS chosen side, EV=1-edge/step. **TWO PROTOCOL/SAGA EXTENSIONS (S25 reuses, don't re-build):** (1) `StatefulGame.step` gained `rng: RngStream` (symmetric to init) — Mines.step ignores it (`del rng`), HiLo.step draws 1 card/guess; the HmacRngStream is COUNTER-INDEXED so the saga reconstructs it per /action via HmacRngStream(seed,clientSeed,nonce,cursor=stored) — seed stays in server_seeds table, NEVER copied into server_state (rejected alts: pre-committed bounded sequence=reachable cap; seed-in-state=S7 security regression). (2) server_state is now the ENVELOPE `{cursor, game:<opaque>}` — saga reads ONLY cursor+game, passes `game` to init/step/public_view, never indexes inside game (opaque seam holds for Mines too). (3) NEW `StatefulGame.public_view(state)->dict` = the GAME's own client-safe projection, wired at BOTH /bet-open AND /state — surfaces HiLo's shownRank (playability/spec API), redacts Mines layout while ACTIVE / discloses at TERMINAL. **Protocol now COMPLETE: init, step(+rng), validate_input, public_view (all `...`, no no-op defaults).** Rounding: cumulative floored ONCE at cashout (cap after). cursor read→step→write all inside FOR UPDATE (no lost-update/same-card replay). Migration d4e5f6a7b8c9 (down_rev=c3d4e5f6a7b8, HiLo seed only, NO DDL — reuses S12 server_state col, reversible, linear). SEED: edge=0.01 params={}; GameLimit GOLD min_bet=1 max_bet=100_000_000 max_win=1_000_000_000 max_multiplier=1_000_000 (informational — real cap is max_win via engine.money.cap; cumulative unbounded in principle). Fix-cycle 2 added public_view (worker initially mis-scoped it as outside the engine file set). NON-BLOCKING carry-forwards: (a) max_win→0 fallback if limit row vanished mid-round (unreachable); (b) _stateful_replay (/bet resend) returns transition `outcome` not public_view (no leak — ACTIVE transition details never carry layout); (c) NO HiLo-specific concurrency test (code correct by inspection: cursor mutation inside FOR UPDATE; inherited Mines lock test is the proven mechanism but Mines doesn't advance cursor — add a HiLo lost-update test when revisiting realtime/poker concurrency). Verify: hilo 37 + mines 86 + full suite 420 passed/42 desel; merged-tree purity-empty/ruff/mypy(40)/lint-imports(2)/registry-9/single-head; 104 pure tests |
| S15 | Keno (tuned table) | passed | 1 | casino-games/S15 | 99ccc05 | merged→061ca8f (registry.py conflict resolved: keno entry + default_params import kept alongside all 6 prior). engine-purity PASS (reviewer hand-verified all 30 analytic hypergeometric cells, worst-dev 1.92e-4 at MED pick-2 within ±0.2%; sampling unbiased+exactly-k; verifier-parity non-vacuous). §A.8 draw 10 of 40; 30 cells risk{LOW,MED,HIGH default MED}×picks{1..10}, analytic RTP 0.99. **CARRY-FORWARD: added engine/src/engine/sampling.py sample_without_replacement(rng,pop,k) (partial Fisher-Yates, consumes exactly k draws) → S12 Mines REUSES this (don't duplicate).** registry.py carries import (default_params) + entry. RULED: LOW/HIGH risk are acceptable extension (default MED matches spec §A.8; all tiers tuned+analytically tested), NOT scope creep. SEED: edge=0.01 params tables; top HIGH pick-10=57551× → maxMultiplier≥60000, minBet=1 maxBet=100_000, maxWin policy call (suggested 5e9). Merged-tree verify: 45 passed/4 desel + heavy 4 passed; ruff/mypy(38)/lint-imports(2 kept)/purity-empty; registry loads all 7 |
| S16 | Roulette 0–99 | passed | 1 | casino-games/S16 | 0b432b4 | merged→997d178 (registry.py conflict resolved: both pocketdice+roulette entries kept). §A.9 floor(f*100); colour from cfg.params["pockets"]={GREEN:[0],RED:[1..49],BLACK:[50..99]}; multi-bet → single stake-weighted mult Σ(stakeᵢ·mᵢ)/Σstakeᵢ. engine-purity PASS. SEED-MIGRATION FLAGS: (1) GameConfig MUST carry params.pockets (missing→KeyError/500, reviewer-blessed as ops-misconfig not client err). (2) green pays 99× → maxMultiplier≥99 AND maxWin≥maxBet·99 or a legit green win is capped & silently depresses RTP (uncapped harness won't catch). (3) live bet loop must debit stake=Σbets[].stakeMinor. APP-LAYER ADVISORY: per-bet payoutMinor display floor(Σ)≠Σfloor (engine carries un-floored multipliers, single floor over total — correct; flag when wiring per-bet display). edge=0.01. Verify: 29 passed/4 desel + heavy 4 passed, ruff/mypy(35)/lint-imports/purity-empty |
| S17 | Optional extensions | pending | 0 | — | — | OPTIONAL — skip if descoping |
| S18 | Crash round actor + WS | passed | 2 | casino-games/S18 | 8d51008 | merged→8afbf9c. **FIRST REALTIME game.** Pure sync core `app/ws/crash_core.py` (CrashRound frozen dataclass + ordered WAITING→LOCKED→RUNNING→CRASHED→SETTLED via `_transition` raising InvalidTransition) behind thin async shell `app/ws/crash.py` (CrashActor timed loop, `secrets.token_bytes(32)` seed-gen in app/ NOT engine, SupportsPublish/CrashBroker protocols, InMemoryPubSub + RedisPubSub, WS gateway /games/originals.crash; main.py includes the WS router only — NO loop on boot). C=crash_point(f,edge) reuses **S10 _curve.py CF VERBATIM**; per-round f=create_rng(roundServerSeed, client_seed=roundId, nonce=roundNumber).next() → byte-identical to `verifier.reproduce_round(server_seed, round_id, round_number)` (S7 round mode). **Instant-bust rate = P(f<edge)=edge (NOT the floored P(C==1.00)≈2·edge trap).** Commit-before-reveal: WAITING `round` event carries serverSeedHash only (no seed, no C); `crash` event reveals seed that SHA-256s to the commitment. Cosmetic ticks presentation-only (m<C always), never decide outcome. NO betting/credit path yet (→S19). Reviews: fairness-rng PASS; realtime-integrity PASS after cycle-2 (test-only 8d51008: bound the transition guard + instant-bust-zero-ticks — BOTH proven non-vacuous RED-on-deletion by the reviewer independently: neutering `_transition` → 3 illegal-transition tests fail "DID NOT RAISE"; regressing run loop `>=`→`>` → instant-bust test fails with a leaked 1.00× tick). Merged-tree verify: purity-empty, ruff pass, mypy(43) clean, lint-imports(2 kept/0 broken), 18 crash tests passed/1 deselected (rtp_heavy). **S19 NOTE: persist the round tuple (round_server_seed, round_id, round_number, C) at round START; reuse GameRound (MULTIPLAYER discriminator) + nullable per-round-seed column/JSONB, NOT a parallel crash table; migrate DEFAULT_CRASH_EDGE=0.01 into DB GameConfig.** |
| S19 | Crash betting + cash-out | passed | 2 | casino-games/S19 | ae63b9a | merged→d2db48a. SECURITY-GRADE — realtime-integrity PASS + ledger-security PASS. **Dedicated Crash money path `app/ws/crash_bets.py`** (open_crash_round, place_crash_bet, manual_cashout, settle_crash_round, _settle_bet) — Crash is a SHARED actor-owned MULTIPLAYER round (NOT a per-user StatefulGame: each bet own betId, no per-user nonce, no one-active-round guard), so it does NOT go through place_bet. **REUSES bet_loop money atoms VERBATIM** (Ledger.debit/credit, _idempotency_key, _latest_enabled_config, GameLimit/Wallet lookup, RG can_bet) — reviewer confirmed NO second key scheme / debit path / balance read; money semantics identical to bet_loop (Σ=0 reconcile verified over mixed round + idempotent re-sweep + duplicate cashout). Single debit hash(betId,WAGER) at placement during WAITING; single credit hash(betId,WIN) at win. **FOUR integrity constraints (advisor-shaped):** (1) single terminal transition per bet — _settle_bet locks Bet FOR UPDATE, checks status==ACTIVE before transition, credits OUTSIDE lock (mirrors bet_loop.step_action:726) → manual WIN never overwritten LOST by crash sweep; (2) two DISTINCT inequalities — auto WINS iff t≤C INCLUSIVE pay t×, manual WINS iff stamped m<C STRICT pay m× else LOSS (both boundaries tested vs actual C); (3) NO client-time cashout — manual multiplier is the actor's last PUBLISHED tick, stamped server-side via injected clock/tick seam, cash_out takes only betId; (4) per-bet idempotent crash sweep (auto-target from persisted BET_PLACED audit) so S20 re-settle is a pure replay. Round tuple {roundSeed, roundNumber, C} persisted at START in GameRound.server_state (MULTIPLAYER discriminator) — NO DDL. Migration **f6a7b8c9d0e1** (down_rev e5f6a7b8c9d0, linear, reversible — proved upgrade+downgrade+re-upgrade) seeds authoritative originals.crash GameConfig edge=0.01 + GOLD GameLimit min_bet=1 max_bet=100_000_000 max_win=1_000_000_000 (max_win is an explicit documented product cap, not an RTP depressor; common low-mult cashouts uncapped). No engine change (purity empty). Cycle-2 TEST-ONLY (ae63b9a): both reviewers found integrity-critical guards UNBOUND (deleting them left suite green — timing gates had zero rejection tests; _settle_bet guard passed vacuously via settle's ACTIVE-filtered SELECT). Worker added 7 tests (5 timing-gate rejections + 2 direct _settle_bet conflicting-decision); both reviewers re-verified NON-VACUITY independently (neuter→RED→restore). **SCOPE FENCES (both reviewers ruled OK): raw WS {type:bet}/{type:cashout} frames DEFERRED until WS auth/session identity exists; stale-seed-on-restart (default id_factory round-{n} no-op reuses stale seed) is S20 — DORMANT (no persistent CrashActor wired into run_forever in app/).** Merged-tree verify: purity-empty, ruff pass, mypy(44), lint-imports(2 kept/0 broken), 49 crash+ledger+bet-loop tests passed/1 desel, single head f6a7b8c9d0e1. **S20 CARRIES: (a) wire the persistent CrashActor + GET /games/originals.crash/state resume + deterministic restart re-settle from committed seed + close the stale-seed id_factory hazard; (b) reconciler for bets stuck non-terminal past TTL; (c) latency-fairness by server timestamp; (d) raw WS bet/cashout frames need WS auth — flag if S20 still lacks it.** |
| S20 | Crash recovery + latency-fairness | passed | 1 | casino-games/S20 | 26b30c2 | merged→21d3456 (S20 feat=375ca92+26b30c2; branch also merged main's de-flake d07f4f5). **P3 CRASH COMPLETE.** SECURITY-GRADE — realtime-integrity PASS + ledger-security PASS, reviewers run SERIALIZED (process lesson from S19). Recovery REUSES the single S19 idempotent settle path (settle_crash_round→_settle_bet) for BOTH restart re-settle AND the TTL reconciler — NO parallel money path (ledger reviewer confirmed; Σ=0 verified across a DOUBLE-recover probe; one WIN row per winning bet). New `app/api/crash.py` GET /games/originals.crash/state: resume current shared round + CALLER's active bets (token identity, reuses /games auth). **STATUS-DEPENDENT redaction (the catastrophic invariant, probed NON-VACUOUSLY by realtime reviewer — forcing is_settled=True fails the redaction tests):** while ACTIVE exposes only roundId/roundNumber/serverSeedHash/status/cosmetic-multiplier, WITHHOLDS C + raw seed (server_state NEVER serialized); seed+crashPoint disclosed only once SETTLED (post-reveal). Identity fence: only Bet.user_id==caller (2-user test). **Stale-seed hazard CLOSED restart-safely:** next_crash_round_number=max(nonce)+1, CrashActor.recover() re-settles orphans then seeds the counter, run_forever recovers on entry (NOT on import — main.py includes the /state router only; dedicated process runs the loop). Collision test exercises id-reuse (persist round-{n}+bets, fresh actor → does NOT reuse id, returns n+1, orphan re-settles deterministically against committed C). Reconciler reconcile_crash_bets (TTL=DEFAULT_RECONCILE_TTL_SECONDS=300 config, recover_crash=ttl 0) selects rounds with stuck ACTIVE bets that are terminal OR past TTL; a genuinely fresh live round excluded (bound test). Latency-fairness KISS: resolution by SERVER stamp, no client timestamp, NO ordering queue (cash_out takes only betId). Recovery-time C re-derivation via crash_point_for_round asserted ==stored C kept TEST-LEVEL (both reviewers ruled OK: server_state stores no edge, a prod re-derive would false-fire on edge-config change; settlement uses persisted C). No engine change (purity empty); NO migration/DDL (reads existing cols). **NON-BLOCKING FOLLOW-UPS (carry to later steps): (a) persist per-round `edge` alongside {roundSeed,roundNumber,C} so the verifier can re-derive C in PRODUCTION (today only test-level); (b) periodic reconciler has NO scheduler caller yet — boot recovery is wired, periodic sweep is a tested ops seam awaiting scheduler infra; (c) BET_PLACED query in crash_state is round-scoped (over-fetches payloads) but no cross-user leak (identity fence holds via PK lookup) — tidy later; (d) raw WS {type:bet}/{type:cashout} frames STILL await WS auth/session identity (deferred since S19).** Merged-tree verify: purity-empty, ruff pass, mypy(45), lint-imports(2 kept/0 broken), 47 crash tests passed/1 desel, single head f6a7b8c9d0e1. |
| S21 | Slots framework engine | passed | 2 | casino-games/S21 | b791625 | merged→228111b. P4 ROOT. engine-purity PASS (cycle-2 test-only). Pure SlotMachine engine/slots/framework.py: config-driven (reels×rows, weighted strips, paytable, paylines OR ways, wild/scatter); play = exactly 1 rng draw/reel → stop=floor(next()·len(strip)) → cyclic grid window → evaluate lines/ways(+wild sub)+scatter → total mult. RTP emerges from strips+paytable, NO clamp. NO feature modules (S22), NO machine/registry/endpoint (S23 — SlotMachine has no id yet, "designed to conform to InstantGame"). **NORMALIZATION CONVENTION (S23 RTP-gate author MUST honor): per-line mult → Σ line_mult/num_lines (stake split across lines); ways+scatter ride total bet, added directly.** Cycle-2 bound 2 untested wild paths (ways-mode wild sub; all-wild-line fallback) — both non-vacuous (reviewer re-proved RED-on-break). Verify: 14 passed; purity-empty/ruff/mypy(49). **S22 NEXT (eligible): slot feature modules — free spins/hold-and-spin/pick bonus, pure modules the framework calls.** |
| S22 | Slot feature modules | passed | 1 | casino-games/S22 | f6a2d1d | merged→a1d1704. engine-purity PASS. engine/slots/features.py: free-spins (retrigger via a drawn prize-strip entry carrying a spin count), hold-and-spin (lock/respin/reset-on-new-lock/decrement), pick-bonus (without-replacement choose-to-reveal) + trigger_fires (scatter {symbol,count} over base grid) + apply_features dispatcher. ONE guarded hook in SlotMachine.play (no "features" key → no-op, S21 provably unregressed — 14 S21 tests green, base-spin stream unshifted, features draw AFTER reel stop from the SAME stream). Multiplier contributions on total bet, RTP from config (index=floor(f·len(strip)) weighting), no clamp; maxSpins bounds COUNT not multiplier. Non-vacuous (mutation probes on retrigger + lock-reset fail their tests). NO registry/machine (S23). Verify: 36 passed; purity-empty/ruff/mypy(55). |
| S23 | Machine #1 + RTP gate | passed | 1 | casino-games/S23 | c4a49a0 | merged→9253d09. engine-purity PASS (independent reviewer re-ran all gates). First concrete machine on the S21/S22 framework. machine01 (5×3, 10 lines, retrigger-capable free-spins) ships as INERT data in engine/slots/machines/machine01.json — engine purity forbids engine open(), so loaded ONLY app-side (app.slots.load_machine) + seeded into GameConfig.params by reversible migration a7b8c9d0e1f2 (down_rev f6a7b8c9d0e1, up/down/up proven). **DB-seeded-from-JSON is the SOLE runtime authority — no Python-literal params copy to drift, so NO Originals-style no-drift test transfers (deliberate).** Registry entry slots.machine01→engine.slots.framework with EMPTY params + module-level GAME singleton; engine never branches on id → ONE framework serves N machines (S24 = new registry entry + JSON + config, NOT code). POST /games/{id}/spin = thin bet-loop alias. **THE GATE'S MEANING = THE PARITY TEST: one numpy _simulate both produces the RTP number AND is proven EXACTLY equal (grid+multiplier) to engine SlotMachine.play under the SAME uniform feed (list-replay RngStream stub mirroring draw order: 5 reel stops then 1 draw/played free-spin), NOT same-seed. Sample FORCES feature (≥1200), retrigger (≥300, variable draw count), all-wild (≥1) — each min-count-guarded so coverage can't silently vanish. Genuine RED found in dev: feature-contribution associativity (line_part+feature_total).** Tolerance = variance-aware 5σ CLT half-width z·√(Var/n) on empirical per-spin variance (~12.6); RTP 0.96 from strips/paytable, NO clamp, NO widened band. Heavy 1e8 @pytest.mark.rtp_heavy (CI rtp-gate auto-joins); fast ~1e7 unmarked. numpy added to **dev group ONLY** (never imported by engine/ or app/ runtime). Verify on merged tree: 673 passed/54 deselected; purity-empty/ruff/mypy(57)/lint-imports(2); single alembic head a7b8c9d0e1f2. **S24 note (from reviewer): _simulate/_analytic_rtp are hardcoded to machine01's shape (lines mode, single freeSpins feature, strip0-only frequencies — valid only because all 5 strips are identical here); S24's machines need these extended.** |
| S24 | Machines #2–3 | passed | 1 | casino-games/S24 | 7e661d2 | merged→c4fd1e9. **🎉 CLOSES P4 SLOTS.** engine-purity PASS (independent reviewer confirmed the CENTRAL CLAIM via git diff --name-only: engine/slots/framework.py + features.py UNCHANGED). machine02 "Aurora Drips" (LOW vol, var~6.3: 2-of-a-kind pays, gentle free-spins, base-dominated) + machine03 "Vault Breaker" (HIGH vol, var~103: only 4/5-of-a-kind pays, W5~816×, mostly-zero prize strip w/ rare huge retrigger, feature-dominated). A test ASSERTS var_high > 3× var_low (actual 16.4×); ordering m02(6.3) < m01(12.8) < m03(103). Diff = 2 inert JSON configs + 2 additive registry entries (empty params → engine.slots.framework.GAME) + reversible migration b8c9d0e1f2a3 (down_rev a7b8c9d0e1f2, up/down/up proven, seeds GameConfig.params FROM JSON via load_machine, leaves m01 untouched) + tests. NO framework/feature edit, NO endpoint dup (generic /spin alias serves all). Both target RTP 0.96; shape authored by hand then single uniform paytable rescale k=0.96/analytic → EXACT analytic 0.95999/0.96000 (config tuning, NOT a clamp; genuine RED first: detuned configs failed the gate). Per-machine parity gate mirrors S23 (exact grid+mult equality, same uniform feed via list-replay stub, FORCES feature≥1200/retrigger≥300/all-wild≥1 — observed 1614/931/50 & 1617/975/50). Tolerance = variance-aware 5σ CLT z·√(Var/n); heavy 1e8 @rtp_heavy, fast 1e7 unmarked. numpy in dev group ONLY. Verify on merged tree: purity-empty/ruff/mypy(57)/lint-imports(2); single head b8c9d0e1f2a3. **NON-BLOCKING FOLLOW-UP: tests/slots_sim.py (new shared sim harness) duplicates machine01's still-inline _simulate and its docstring overclaims m01 reuse — test-scaffolding DRY smell only (each copy independently pinned to engine by its own parity gate; engine outcome logic still single-owner). Migrate m01 onto slots_sim / fix docstring later.** |
| S25 | Blackjack | passed | 2 | casino-games/S25 | fe1eef0 | merged→b186ab5 (registry+table/__init__ UNION w/ roulette+baccarat+video_poker — 4 table games + 9 originals = 13 total). engine-purity PASS (cycle-2 test-only). **First StatefulGame TABLE game.** D-deck shoe committed at open, drawn WITHOUT replacement from stream (init 4 draws; hit/double/split/dealer each 1 rng.next(); stand/insurance/surrender/peek 0) → verifier-reproducible. Defaults: 6 decks, dealer S17 (configurable), BJ 3:2, DAS, split to 4, split-aces-1-card, dealer peek, no surrender (configurable), penetration 0.75 (reshuffle a documented seam, not built). Payouts rule-FIXED; cfg.edge=0.004 EMERGENT/doc-only. Reuses COMPLETE StatefulGame protocol. **public_view redaction (worker stalled mid-stream once, resumed clean): ACTIVE surfaces dealer UP-card + own hands + legalActions, OMITS hole card AND undrawn shoe (shoe leak = hole-card leak by deduction over known deck) — cycle-2 bound the shoe-omission via EXACT key-allowlist set(view)=={...} (junk-key probe also fails; mirrors S28).** EV gate: basic-strategy E[net]/initial-bet ≈ published −0.40% within CLT 5σ z·√(Var/n); heavy n=2e6 behind rtp_heavy + fast n=120k; measured ≈−0.0046. Hypothesis property tests (bust/BJ/push/payouts/split/double/dealer-draw) non-vacuous (dealer-S17 probe fails). Verify: 26 passed/1 desel + heavy 1 passed; purity-empty/ruff/mypy(54). **Follow-up DONE: types.py StatefulGame docstring corrected (Baccarat→Video Poker) @1cd2a3b.** |
| S26 | European Roulette (wheel) | passed | 1 | casino-games/S26 | 4255c99 | merged→2711d04. engine-purity PASS. InstantGame, game id **table.roulette** (DISTINCT from originals.roulette — both coexist, registry-verified). pocket=floor(rng.next()*37)∈0..36; all inside/outside bets via fair payout table (straight 35 … even-money 1); per-bet RTP=(count/37)·(payout+1)=36/37 BY CONSTRUCTION (pocket 0 = 1/37 edge; outside covers exclude 0) — from table not clamp; cfg.edge=1/37 is doc only. Multi-bet stake-weighted Outcome (mirrors roulette99). **Count fence is load-bearing + non-vacuous** (reviewer neutered BOTH count enforcers → STRAIGHT[1,2]/SPLIT[1] "DID NOT RAISE"); adjacency NOT validated by design (RTP depends only on cover COUNT; non-adjacent selection stays negative-EV — no arbitrage; adjacency is client/UX). Heavy RTP gate: 5 passed @1e7 (uniformity 5σ binomial p=1/37 + 4 inside tiers via shared run_rtp 5σ CLT). **CARRY observation (cross-cutting, NOT S26-specific — also in roulette99): aggregate Σ(stakeᵢmᵢ)/Σstakeᵢ floored once can underpay 1 minor unit on non-terminating ratios (e.g. 3·(2/3)→1 vs 2); blessed pattern, file a separate ticket if it matters.** Merged-tree verify: purity-empty, ruff, mypy(47), lint-imports(2), 54 passed/5 desel. |
| S27 | Baccarat | passed | 1 | casino-games/S27 | b76004a | merged→badf46b (registry conflict w/ S26 resolved additively — table.roulette + table.baccarat both kept). engine-purity PASS. **InstantGame** (no player decisions; reviewer flagged engine/types.py StatefulGame docstring still lists Baccarat as an example — STALE, refresh the spine docstring separately). §B.4: deal P/B from finite 8-deck shoe (sample w/o replacement, 1 rng.next()/card), FIXED third-card tableau, settle P/B/TIE stake-weighted (roulette pattern). Banker win 1:1−commission(0.05); Tie 8:1; P/B push on tie. **Edges EMERGENT, certified by EXACT 8-deck depletion enumeration driven by the engine's OWN predicates: P=0.446247/B=0.458597/T=0.095156 → Banker 1.06%/Player 1.24%/Tie 14.36%** (finite-shoe model — infinite-deck wrongly gives Tie 14.12%). ONE resolution path shared by play()(rng)+enumeration. Non-vacuous (perturbing a banker-draw threshold failed 3 anchors; shoe proven true no-replacement permutation). edge=0.0106 doc-only. Verify: 34 passed/3 desel + heavy 3 passed; purity-empty/ruff/mypy(47). |
| S28 | Video Poker (+ shared evaluator) | passed | 1 | casino-games/S28 | b1dd32a | merged→de48c49 (registry+table/__init__ conflicts resolved by UNION: table.roulette+table.baccarat+table.video_poker all kept). engine-purity PASS. **CARRY-FORWARD DELIVERED: engine/cards/evaluator.py — `rank_five(cards)->HandRank` frozen order=True dataclass (category, tiebreak) = TOTAL ORDER, so S29 best-5-of-7 = `max(rank_five(combo) for combo in combinations(7,5))` reusing verbatim (7-card path NOT built — YAGNI, not walled off). GAME-AGNOSTIC: returns "one pair @ rank X" never "jacks-or-better" (min-paying rule lives in video_poker); royal=ace-high SF; ace dual-role (wheel+broadway). S29 MUST reuse this, never re-rank.** StatefulGame: init commits 10 cards (5 dealt+5 replace) via sample_without_replacement, draw action holds, public_view whitelist {status,dealtCards} hides pool while ACTIVE. Paytable-driven 9/6 JoB, no clamp. **RTP-GATE RULING (reviewer-blessed): published 99.54% INFEASIBLE to confirm by pure-Python sampling (C(52,5) optimal EV/deal; 800× royal floors sampled tol ~0.7%) → gate is EXACT ENUMERATED per-fixture EV (four-to-royal=872/47 @ rel=1e-12, exercising shipped evaluator+paytable); rtp_heavy is a labeled coarse sanity band (0.96<rtp<1.00) NOT the gate. Accepted per step's optimal-for-fixtures allowance + spec.** Verify: 34 passed/1 desel + heavy 1 passed; purity-empty/ruff/mypy(53). |
| S29 | 7-card hand evaluator | passed | 1 | casino-games/S29 | 2a18712 | merged→ed2dbff. **Opens P6 Poker.** engine-purity PASS. rank_seven(cards)->HandRank = exactly `max(rank_five(combo) for combo in combinations(cards,5))` over C(7,5) — CARRY-FORWARD honored: reuses S28 5-card core verbatim, re-implements ZERO category logic; all 9 HandRank(HandCategory.*) constructions remain solely in rank_five (single ranking owner, reviewer-confirmed via grep). Returns only the totally-ordered HandRank (no winning-cards bloat, ISP/YAGNI); HandRank @dataclass(order=True) makes showdown >/== free for S30/S31. Input fence: exactly 7 DISTINCT cards (both reject paths tested). Tests: 9 known-hand oracle cases through the 7-card entry (royal/wheel/quads-from-7/two-trips→higher-FH/flush-over-straight/6-run-beats-wheel/best-5-not-obvious/splits/play-board-tie) + 400 seeded random-deal property checks (result>=every 5-subset AND total-order/transitivity/monotonicity). Genuine RED (ImportError pre-impl). random.Random(seed) test-only. No registry/migration (pure evaluator). Verify on merged tree: 419 passed; purity-empty/ruff/mypy(57)/lint-imports(2). **S30 NEXT (eligible): engine/poker/pots.py main+side-pot engine — the classic bug source; Hypothesis chips-in==chips-out conservation under multi-all-in.** |
| S30 | Pot / side-pot engine | passed | 2 | casino-games/S30 | 41cd208 | merged→90020d3. engine-purity PASS (cycle-2 test-only). engine/poker/pots.py (new package), pure + integer-only. build_pots(contributions,folded)→list[Pot]: main pot + one side pot per distinct all-in level (level L collects (L-prev)·#in-for-≥L; eligibility=non-folded contributors at ≥L; folded chips stay IN but folders eligible nowhere). award_pots(pots,ranks)→dict[seat,chips]: best eligible hand(s) per pot, splits ties. INVARIANT chips-in==chips-out independently traced by reviewer + Hypothesis 1000 examples (asserts MORE than sum-eq: recipients non-folded/⊆eligible/positive). SINGLE RANKING OWNER via ISP seam: award_pots takes pre-computed _Comparable rank per seat — pots.py ranks NOTHING; a test threads REAL rank_seven through it exercising both winner-takes-all (p0>p1 asserted) + genuine tie (t0==t1 asserted). Odd-chip: amount%k leftover → one each to LOWEST seat ids among tied winners (deterministic, pinned by test; button-relative override = named S31 seam). Orphaned pots cascade DOWN to next awardable (eligibility monotone in level → empties are contiguous top block), never to folder; no-eligible-anywhere = deliberate ValueError. **Cycle-1 (CHANGES_REQUESTED): reviewer traced algo CORRECT but 3 guards unbound (deletable w/ suite green) — silent-chip-loss ValueError, _check_amount (float/bool-as-int/negative), missing-rank guard; fixed TEST-ONLY w/ RED→restore→GREEN per guard, pots.py byte-identical.** Verify: 17 passed; purity-empty/ruff/mypy(59)/lint-imports(2). NOTE: transient ENOSPC during fix (cleared; restoration verified). **S31 NEXT (eligible): engine/poker/table.py pure NLHE reducer — blinds/button, 4 betting rounds, legal-action validation, showdown via evaluator+pots.** |
| S31 | Poker table reducer (engine) | passed | 1 | casino-games/S31 | f7507ed | merged→bcc0779. **The NLHE rules engine — spine of P6.** engine-purity PASS (NL rules reviewed hardest dimension, all correct). engine/poker/table.py (468L) pure reducer: frozen TableState/Seat/Action; deal_hand(config,stacks,button,rng)→state, start_next_hand (rotate button+carry stacks), apply_action(state,action)→state (raises IllegalAction WITHOUT mutating; validate-before-replace; immutability structural via frozen dataclasses). Integer minor units. SINGLE-OWNER reuse: showdown=rank_seven; pots=build_pots/award_pots; deal=sample_without_replacement from INJECTED RngStream (ONE entropy consumer is deal_hand; apply_action takes NO rng — betting has no per-action entropy, mirrors Mines init-commit). grep confirms NO re-implemented ranking/side-pot math. NL CORRECTNESS (reviewer-verified): min-raise via last_full_raise (reset to BB/street, below-min rejected); PARTIAL all-in (incr<last full raise) raises current_bet but does NOT reopen (already-acted may only call/fold, non-vacuously asserted); BB preflop option (is_open=current_bet==0); _needs_to_act round-close traced correct on limped-preflop+checked-around (no off-by-one); out-of-turn/check-facing-bet/over-stack/call-when-matched rejected state-unchanged; heads-up button posts SB+acts first preflop. Chip conservation: per-seat stack+total_contrib invariant; all-fold=_award_uncontested whole-pot push. DETERMINISM/REPLAY (load-bearing for S32 recovery): same seed+actions→identical deal+result; full action-log replay reproduces frozen state exactly. Hole-card isolation for S32: Seat.hole + community_undealt are the ONLY secret fields, cleanly redactable per seat. Tests: 16 (scripted multiway + all-in side-pot vs hand-computed oracles; illegal-action reject+state-unchanged; rotation incl heads-up; determinism+replay). Verify: 16 passed (full suite 1055 passed/84 skipped); purity-empty/ruff/mypy(60)/lint-imports(2). **NON-BLOCKING follow-up: table.py docstring (~L26-28) claims uncalled-bet return flows via award_pots lone-eligible top pot, but the only uncalled-bet TEST is the all-fold _award_uncontested path (never calls award_pots); mechanism sound+covered at S30 but not exercised here — reconcile comment or add over-bet-to-showdown test later.** **S32 NEXT (eligible, SECURITY-GRADE, depends S31+S18): app/ws/poker.py WS actor — per-seat redaction (NO hole-card leak), Redis state checkpointed to PG, recover via deal-from-seed+action-log replay.** |
| S32 | Realtime poker + redaction | passed | 1 | casino-games/S32 | 929e931 | merged→42c63b6. **SECURITY-GRADE PASS (realtime-integrity reviewer tried to break leakage along every path, could not).** app/ws/poker.py: one authoritative PokerActor/table drives the pure S31 reducer (client sends intent only; action intake routes through apply_action — no re-implemented rules). REUSES Crash S18-20 seams VERBATIM (SupportsPublish/InMemoryPubSub/RedisPubSub/broker pump) + a PokerStateStore seam (InMemory/Redis/Postgres). **NO-LEAK BY CONSTRUCTION: redaction at SOURCE — full TableState NEVER crosses the broker; actor publishes a separately-redacted view per seat channel (poker:{t}:seat:{n}) + spectator via pure ENGINE WHITELIST engine/poker/table.py::public_view(state,viewer_seat) (+66L, stays stdlib-pure; matches StatefulGame.public_view convention). NAMES public fields; NEVER references community_undealt (can't leak by omission); hole emitted only for own seat OR genuine showdown reveal (hand_over AND >=2 live AND not folded). Folded/mucked NEVER revealed; uncontested win reveals nothing.** FAIRNESS DEVIATION (deliberate, reviewer-endorsed): serverSeed NEVER revealed (only serverSeedHash=commit(seed) published) — revealing a poker deal seed would reconstruct mucked holes & defeat redaction; per-card-commitment fairness = documented deferred seam; poker not on /fairness/{betId} so no seed exposure. Out-of-turn/wrong-seat/unknown-kind → error to ONLY offending seat, no state in payload, table unmutated. Checkpoint=replay tuple {seed,blinds,startingStacks,button,handNumber,actionLog}; reload reconstructs IDENTICAL frozen state via deal-from-seed+action-log replay (S31 property); seed server-side only. NO money moves (S33). Uses pre-existing poker_tables JSONB (S1 schema; NO migration). Tests: test_poker_ws.py 8 passed + test_poker_public_view.py 5 passed; _assert_no_leak iterates EVERY payload×EVERY channel structurally, covers all streets+showdown incl folded seat, saw_reveal asserted (non-vacuous); naive-broadcast RED fires. Verify: purity-empty/ruff/mypy(61)/lint-imports(2). **✅ PG durable test (test_postgres_checkpoint_store_survives_reload) NOW CONFIRMED against live Postgres 2026-06-28 after Docker recovery — test_poker_ws.py 9 passed (was 8+1 PG-skip).** NON-BLOCKING: naive-broadcast RED short-circuits on communityUndealt assertion (proves only that detector directly; hole-leak branches armed by headline GREEN full-log scan per reviewer) — split the RED later. **NEXT: {S34 timers/disconnect/reconnect — DB-FREE testable, dispatch NOW} ; S33 buy-in/rake HELD (ledger, REQUIRES Postgres — blocked on Docker).** |
| S33 | Matchmaking + buy-in + rake | passed | 2 | casino-games/S33 | 5365f95 | merged→5365f95 (--no-ff). **SECURITY-GRADE PASS (ledger) after FIX CYCLE 1 closed a reproduced MONEY-PRINTING exploit.** app/poker/lobby.py bound to existing Ledger: buy-in debits player→house, leave credits remaining stackMinor house→player, take-rake debits floor(pot*rate_bps/10000) capped to winner's seat. **Cycle-0 exploit (caught+reproduced end-to-end):** BUYIN idem key hash(tableId,handNo,seat,opType) carried no occupant identity & hand_no never advances, so reseating player B into A's just-VACATED seat made B's BUYIN key collide with A's → ledger REPLAYED A's debit → B seated FREE then extracted via leave-credit. **FIX (cycle 1):** BUYIN/LEAVE now key on hash(tableId,seat,occupancyId,opType) where occupancyId is a monotonic-per-seat counter minted each sit-down (vacated/empty→prev+1, fresh→0), reused only by the SAME occupant's re-send → 2 distinct occupants never collide, same-occupant re-send still collapses on the ledger UNIQUE fence; RAKE keeps hash(tableId,handNo,seat,opType) (once/hand). Buy-in reworked into 3-phase saga (reserve zero non-creditable seat→debit→finalize real stack), fails toward house; `paid` flag → finalize fires exactly once so a buy-in replay can't resurrect a diminished stack (2nd money-print vector worker self-found+closed), leave credits only a paid seat. **15 tests** incl RED-first seat-reuse, multi-wallet cross-player conservation, diminished-stack — all FAIL under old key, pass under fix (reviewer proved both by replaying old+new key formula end-to-end: "EXPLOIT CLOSED: B paid a real debit, no chips printed, system balanced"). Gate: pytest 15 passed, ruff clean, mypy strict clean, engine-purity grep empty (engine untouched), poker regression 34 passed. NON-BLOCKING (liveness/documented-seam only, no money defect): concurrent join SeatTaken-instead-of-reseat; stale-leave-after-reseat raises NotSeated; set_stack credit-amount = the deferred hand-result→stack conservation-loop seam. **S35 now ELIGIBLE (S33+S34 both passed).** |
| S34 | Timers + disconnect + reconnect | passed | 1 | casino-games/S34 | b782cf1 | merged→a5766a2. **SECURITY-GRADE PASS (realtime-integrity; reconnect = new leak surface, held).** Extends S32 PokerActor (app/ws/poker.py +133/-3; engine UNTOUCHED). Injected clock seam: now_fn(=time.monotonic)+turn_timeout(=_DEFAULT_TURN_TIMEOUT const, config not literal); tests advance FakeClock, no time.sleep; clock app-only. Timeout→auto-act: check_timeout() fires at now()-turn_started_at>=turn_timeout; faces-bet(street_contrib<current_bet)→auto-FOLD else auto-CHECK; routed through SAME mutation path as manual act (apply_action→_commit: log→state→_mark_turn→checkpoint→broadcast) — no hand-rolled edit, can't illegally check-facing-bet, server-authoritative. Disconnect→sit-out: _drain_sitouts auto-acts a sitting-out seat the instant it's to-act, runs at END of EVERY public entry (start_hand/act/check_timeout/disconnect/reconnect) so absentee never stalls; only mutates the actual to-act seat. Termination guaranteed (each auto-act folds→live↓ or checks→street↑); never-stalls test bounded-ceiling + reaches COMPLETE + chips conserved + pot pushed. RECONNECT NO-LEAK: reconnect() clears sit-out + _send_snapshot re-projects via SAME engine public_view WHITELIST to that seat's channel ONLY (own holes + public, never other holes/community_undealt) — reviewer found no leaking path; test parses serialized type:"state" frame (non-vacuous) + RED guard on naive all-holes projection. Timeout state ephemeral (excluded from checkpoint, re-stamped on from_checkpoint). NO money (S33). Tests: test_poker_timeouts.py 9 passed; poker regression 22 passed/1 skipped (PG durable — Docker down). Verify: purity-empty/ruff/mypy(61)/lint-imports(2). NON-BLOCKING (deferred seam, same as S32): poker_seat_ws not yet wired to call actor.reconnect() (no actor registry) — authoritative logic IS impl+tested at actor seam. **S35 (anti-collusion) needs BOTH S33+S34; S33 BLOCKED on Docker/DB → S35 also gated. P6 fully blocked on Docker until S33 lands.** |
| S35 | Anti-collusion signals | in_progress | 1 | casino-games/S35 | — | security review (realtime-integrity). Worktree off 5365f95. Last P6 step; both prereqs (S33,S34) passed. Worker building app/poker/anticollusion.py (chip-dump / soft-play / shared-IP-device detectors → structured risk-log events, NO auto-bans) + tests/test_anticollusion.py (synthetic collusion fixtures flag; clean fixtures clean — no false positives). |
| S36 | Faucets | pending | 0 | — | — | ledger review (ledger-security-reviewer) |
| S37 | Leaderboards + sinks | pending | 0 | — | — | ledger review (ledger-security-reviewer) |
| S38 | Responsible-gaming stubs | pending | 0 | — | — | |
| S39 | Observability | pending | 0 | — | — | |
| S40 | Load test (sign-off) | pending | 0 | — | — | ⛔ SIGN-OFF · deferred/live · needs S20 (+S35 only if poker built) |
| S41 | Deploy demo | pending | 0 | — | — | ⛔ DESTRUCTIVE/PAID · go required |
| S42 | Portfolio packaging | pending | 0 | — | — | |

> Status values: pending | in_progress | passed | blocked. Note carries review tier, CARRY-FORWARD (CF) constraints, and why a blocked step is blocked.

## Dependency graph
```
P1 Spine        S1 → S2 → S3 ;  S1 → S4 ;  S1 → S5 ;  {S2} → S8 ;  {S3,S4,S5} → S6 → S7
P2 Originals    {S6,S7} → S9(REVIEW GATE) → {S10,S11,S12,S13,S14,S15,S16,S17}   # parallel after S9
P3 Crash        S10 → S18 → S19 → S20
P4 Slots        {S6,S7} → S21 → S22 → S23 → S24
P5 Table        {S6,S7} → {S25, S26, S27, S28}
P6 Poker        S6 → S29 → S30 → S31 → S32(also needs S18) → {S33(+S3), S34} → S35
P7 Polish       S3→S36 ; S3→S37 ; S6→S38 ; S6→S39 ; S20→S40[SIGN-OFF] (+S35 if poker built) → S41[DEPLOY] → S42
```

## Log

- 2026-06-28: **P6 POKER PROGRESS + ⚠️ DOCKER/DB BLOCKER. User chose "P6 then P7" at the P5→next boundary.** P6 landed so far: **S29 evaluator→ed2dbff, S30 side-pots→90020d3, S31 NLHE table reducer→bcc0779, S32 realtime WS+redaction→42c63b6** — all reviewed PASS (S32 security-grade). Integration tip **42c63b6**; engine still pure (mypy 61 files); single alembic head b8c9d0e1f2a3 (poker uses pre-existing poker_tables JSONB from the S1 schema — no new migration). The S28 cards/evaluator carry-forward (rank_seven=max over C(7,5) of rank_five) + the S30 side-pot engine + the S31 reducer + S32 per-seat public_view whitelist are the poker spine; redaction is leak-proof by construction (full TableState never crosses the broker). **⚠️ BLOCKER: Docker Desktop went DOWN mid-session ("unable to start"; earlier S23/S24 DB suites ran fine, so it died later — likely the host disk pressure at ~96-97%, the same ENOSPC the S30/S31/S32 workers hit transiently). Attempted recovery: quit+`open -a Docker`, polled `docker info` 150s — still not ready. IMPACT: (1) S32's ONE Postgres-gated durable test is unverified-against-live-DB (thin JSONB adapter over the already-proven in-memory replay property — re-confirm when Docker returns); (2) the full DB regression suite (ledger/bet_loop/fairness/etc.) can't run; (3) **S33 (matchmaking/buy-in/rake) is HELD — it is ledger-heavy and CANNOT complete its Verify (double-entry/idempotency/reconciliation) without Postgres.** MITIGATION: drove S34 (timers/disconnect/reconnect — DB-FREE testable via in-memory seams + fake clock) NEXT to keep P6 moving around the blocker. NEEDS USER: restart Docker Desktop (and/or free host disk) to unblock S33 + DB regression. S35 (anti-collusion) eligibility depends on S33+S34.** Optional still-open: S17 Originals extensions; cursor lost-update concurrency test for realtime/poker.
- 2026-06-28: **🎉 P4 SLOTS COMPLETE — S21 framework → S22 features → S23 machine01+RTP gate → S24 machines 02/03, all merged. Integration tip c4fd1e9.** The signature claim is PROVEN: "a new slot machine = config + art, not code." Three machines (m02 low-vol var~6.3 / m01 medium ~12.8 / m03 high-vol ~103) all run on ONE unchanged engine.slots.framework.SlotMachine + features.py; each new machine added ZERO framework/feature code — only an inert JSON config under engine/slots/machines/, an additive registry entry (empty params → shared GAME singleton), a reversible migration seeding GameConfig.params FROM the JSON via app.slots.load_machine (engine purity forbids engine open()), and tests. Engine purity held throughout (JSON inert/app-loaded; numpy tests+dev-group ONLY). **The RTP-as-CI gate's MEANING is the parity test: per machine a numpy _simulate both produces the RTP number AND is proven EXACTLY equal (grid+multiplier) to engine SlotMachine.play under the SAME uniform feed (list-replay RngStream stub, NOT same-seed), with min-count guards FORCING feature/retrigger/all-wild spins so coverage can't silently vanish.** Tolerance = variance-aware 5σ CLT z·√(Var/n) on empirical per-spin variance; RTP 0.96 emerges from strips/paytable (tuned, never clamped, never a widened band); heavy 1e8 @pytest.mark.rtp_heavy auto-collected by the CI rtp-gate, fast 1e7 unmarked. Merged-tree gates: 688 tests green (S24 worktree)/673 (S23 merged-tree DB run); purity-empty/ruff/mypy(57)/lint-imports(2); single alembic head b8c9d0e1f2a3. Registry now holds **16 games** (9 originals/stub + 4 table + 3 slots). NON-BLOCKING follow-up (see S24 row): tests/slots_sim.py DRY-duplicates machine01's inline _simulate (docstring overclaims m01 reuse) — cosmetic test scaffolding, each copy pinned to engine by its own parity gate. **🏁 PHASE BOUNDARY: P1–P5 all complete. Remaining major work: P6 Poker PvP {S29–S35} (highest-risk; reuses S28 cards/evaluator best-5-of-7 + S18 realtime foundation; security-grade S32–S35) and P7 Economy/polish/packaging {S36–S42} (faucets, leaderboards, RG, observability, ⛔S40 load-test SIGN-OFF, ⛔S41 DEPLOY destructive/paid, S42 README). P6 can be cut entirely without affecting P1–P5 value. SURFACE TO USER as the next directive: drive P6, drive P7, or stop here.** Optional still-open: S17 Originals extensions (skippable); a cursor lost-update concurrency test pattern for realtime/poker (open since S14).**
- 2026-06-28: **🎉 P5 TABLE & VIDEO POKER COMPLETE — all four table games merged (S25 Blackjack→b186ab5, S26 Roulette, S27 Baccarat, S28 Video Poker). Registry now holds 13 games (9 originals/stub + 4 table); 162 table+slots engine tests green; purity-empty/ruff/mypy(54)/lint-imports(2).** Integration tip **b186ab5** then **1cd2a3b** (types.py StatefulGame docstring fix: Baccarat→Video Poker, since S27 made Baccarat an InstantGame). S25 was the last to land (worker stalled mid-stream, resumed clean; cycle-2 bound the shoe-omission redaction via exact key-allowlist). The S28 cards/evaluator (rank_five total-order) is the live CARRY-FORWARD for P6 poker best-5-of-7. **REMAINING TO CLOSE P4: S22 slot features (in flight, worker a027159280b0f42b3, off de48c49) → S23 machine01 + heavy RTP CI gate → S24 machines 02/03. After P4: P6 Poker {S29..S35} (highest risk; reuses S28 evaluator + S18 realtime) is the next major phase — a future directive.**
- 2026-06-28: **P4+P5 PARALLEL WAVE (user chose "parallelize P4+P5" at the P3→next boundary). Launched 5 concurrent workers off tip 21d3456: S21 (P4 root) + the designated parallel set {S25,S26,S27,S28} (P5 table games).** Landed so far: **S21 Slots framework→228111b, S26 European Roulette→2711d04, S27 Baccarat→badf46b, S28 Video Poker+shared evaluator→de48c49** — all engine-purity PASS, all engine-pure (no DB). Integration tip **de48c49**; 4 table.* + 9 originals.* + slots framework all load; 136 P4/P5 engine tests green; purity-empty/ruff/mypy(53)/lint-imports(2). **S25 Blackjack STALLED mid-stream (recurring long-agent API issue) — worktree had substantial UNCOMMITTED work (blackjack.py 22KB + both test files + registry edit), nothing lost; RESUMED via SendMessage(agentId) telling it to commit-in-small-increments + run Verify. S22 (slot features, P4 next) DISPATCHED off de48c49 to keep BOTH phases moving.** Three serialized registry.py merges (S26→S27→S28) each resolved by UNION — keep ALL additive table.* entries + their imports + close each tuple; the table/__init__.py add/add docstring conflict resolved by keeping the richest wording (S28's, references the shared cards.evaluator). **OPERATIONAL LEARNINGS THIS WAVE: (1) NEVER put backticks in `git ... -m` messages under zsh — `` `word` `` is command-substituted and silently dropped from the commit message (hit twice: S20, S21 merge msgs — both amended; now writing all merge messages to a scratch file + `git commit -F`). (2) Serialized reviewers (NOT concurrent) on a shared worktree remains the rule — but engine-pure P4/P5 reviews are single-reviewer (engine-purity) so no collision. (3) registry.py is the one cross-cutting file across the wave — branch each table game off the LATEST tip when possible, else resolve the union condflict at merge (trivial, additive).** PENDING FOLLOW-UP (hold until S25 merges to avoid a types.py conflict, since Blackjack is a StatefulGame that may touch the example list): refresh engine/types.py StatefulGame docstring — it still lists "Baccarat" as an example though S27 correctly made it an InstantGame. **REMAINING: S25 Blackjack (P5 last, in fix/resume), S22→S23→S24 (P4 chain: S22 in flight, S23 wires machine01+heavy RTP CI gate, S24 machines 02/03). Then P5 fully done + P4 done. P6 Poker {S29..S35} is the highest-risk phase and reuses the S28 cards/evaluator (best-5-of-7) + S18 realtime foundation — a future directive.**
- 2026-06-28: **S20 Crash recovery/reconnect/latency-fairness PASSED both security-grade reviewers (realtime-integrity + ledger-security), merged→21d3456. 🎉 P3 CRASH COMPLETE (S18 actor → S19 betting/cashout → S20 recovery) — the first realtime game is done end-to-end.** Integration tip **21d3456**; single alembic head f6a7b8c9d0e1 (no new migration — S20 reads existing columns). The advisor's framing held: recovery was MOSTLY already built (S19's settle_crash_round is an idempotent replay), so restart re-settle + the TTL reconciler both reuse that ONE money path — ledger reviewer confirmed no parallel settlement routine (Σ=0 across a double-recover). The catastrophic /state-redaction invariant (withhold C+seed while live) was probed NON-VACUOUSLY by the realtime reviewer. **PROCESS LESSON APPLIED: ran the two security reviewers SERIALIZED (realtime first, then ledger) — not concurrently — so their neuter/test/restore probes did not pollute each other's suite runs (the S19 collision). Worked cleanly; keep serializing reviewers that mutate a shared worktree.** ALSO this turn: fixed a PRE-EXISTING S19 flaky test (test_auto_cashout_wins_iff_target_le_C, ~2% — round(C-0.01,2)=0.99 tripped the ≥1.00 placement guard when a random round_id yielded instant-bust C==1.00) on its own branch→merged 8d341a6, then merged main into the S20 branch so the review ran the de-flaked suite. Fix: _open_round gained an optional min_c retry; the test opens with min_c=1.5 so the derived target is ≥1.00 by construction (verified 25/25). S20 carries four non-blocking follow-ups (see the S20 row): persist per-round edge for prod C-re-derivation; wire a scheduler for the periodic reconciler; tidy the round-scoped BET_PLACED over-fetch; raw WS bet/cashout frames still need WS auth. **NEXT: P3 done. Eligible now: P4 Slots {S21 framework → S22 features → S23 machine01+RTP gate → S24 machines 02/03} — INDEPENDENT of P2/P3 (prereqs S6/S7 passed), touches disjoint files (engine/slots/), so it can be driven straight through OR parallelized. Also still available: optional S17 Originals extensions (skippable). P5 Table/video poker {S25 Blackjack, S26..S28} also depends only on S6/S7 and would reuse the S14 public_view + rng-into-step stateful pattern. The user steered "P3 next" at the P2 boundary; P3 is now complete — surface P4 vs P5 vs parallelize for the next directive.** Open since S14: a cursor lost-update concurrency test pattern for realtime/poker.
- 2026-06-28: **S19 Crash betting + server-stamped cash-out PASSED both security-grade reviewers (realtime-integrity + ledger-security), merged→d2db48a. P3 Crash is now bet-playable; only S20 (recovery/latency-fairness) remains in P3.** Integration tip **d2db48a**; single alembic head f6a7b8c9d0e1 (DB-verified at head, reversible). Dedicated Crash money path (app/ws/crash_bets.py) — Crash is a SHARED actor-owned multiplayer round, NOT a per-user StatefulGame, so it bypasses place_bet but REUSES the bet_loop money atoms verbatim (reviewer confirmed no divergent second money path; Σ=0 reconcile verified). Advisor consult up front shaped the four integrity constraints written into the worker brief (single-terminal-transition via FOR UPDATE+ACTIVE guard mirroring step_action; auto t≤C inclusive vs manual m<C strict; server-stamped multiplier via injected clock seam = no client-time cashout; per-bet-idempotent crash sweep for S20 replay). Cycle-2 was TEST-ONLY: both reviewers independently found the integrity-critical guards UNBOUND (deletable with the suite green — the timing gates had zero rejection tests, and the _settle_bet terminal guard passed VACUOUSLY because the existing sweep-overwrite test bound on settle's ACTIVE-filtered SELECT, not the guard). Worker added 7 binding tests (5 timing-gate rejections + 2 direct _settle_bet conflicting-decision); reviewers re-proved non-vacuity (neuter→RED→restore). **PROCESS LESSON: do NOT run two reviewers CONCURRENTLY when both perform reversible neuter/test/restore edits on the SAME worktree — the ledger reviewer observed the realtime reviewer's transient guard-neutering mid-run, which can pollute suite runs; the worktree was verified pristine + both committed guards intact before merge. For future fix-cycle re-reviews on a shared worktree, serialize the reviewers OR give each its own worktree copy.** Migration f6a7b8c9d0e1 seeds originals.crash GameConfig(edge 0.01)+GOLD GameLimit (max_win=1e9 is a documented product cap). No engine change. **NEXT: S20 — Crash reconnect/recovery/latency-fairness (last P3 step; depends on S19; security-grade realtime-integrity). Carries from S19: wire the persistent CrashActor + GET /games/originals.crash/state + deterministic restart re-settle from committed seed + CLOSE the stale-seed id_factory hazard; add a reconciler for bets stuck non-terminal past TTL; latency-fairness by server timestamp; raw WS bet/cashout frames still need WS auth (flag if S20 lacks it). Also still open from S14: a cursor lost-update concurrency test pattern for realtime/poker.** P4 Slots {S21–S24} remains independent of P3 and eligible (prereqs S6/S7 passed) — available to parallelize.
- 2026-06-28: **S18 Crash PASSED both security-grade reviewers (fairness-rng + realtime-integrity), merged→8afbf9c. FIRST REALTIME game live — establishes the actor pattern S19/S20 (and later poker S32) build on.** Integration tip **8afbf9c**. Architecture (advisor-driven): PURE SYNC CORE `app/ws/crash_core.py` (CrashRound + ordered state machine guarded by InvalidTransition; crash_point_for_round reusing **S10 _curve.py CF verbatim**) + THIN ASYNC SHELL `app/ws/crash.py` (CrashActor timed loop, secrets seed-gen in app/, Redis/in-memory pub-sub, WS gateway). Per-round fairness f=create_rng(roundServerSeed, client_seed=roundId, nonce=roundNumber).next() reproduces byte-identically via verifier.reproduce_round → server==verifier holds for the realtime round too. Commit-before-reveal enforced (WAITING carries hash only; crash event reveals seed→SHA256→commitment). Instant-bust correctly P(f<edge)=edge, NOT the floored P(C==1.00)≈2·edge trap. NO betting yet (S19). Cycle-2 was TEST-ONLY (8d51008): the first realtime-integrity pass found the state-machine transition guard + the instant-bust zero-ticks path had ZERO binding tests (guard deletion left all 12 green = TDD violation); worker added 5 binding tests, reviewer INDEPENDENTLY re-proved non-vacuity (neuter `_transition`→3 tests "DID NOT RAISE"; regress loop `>=`→`>`→instant-bust test leaks a 1.00× tick). Merged-tree verify: purity-empty, ruff pass, mypy(43) clean, lint-imports(2 kept/0 broken), 18 crash tests/1 deselected. S18 worktree removed, branch deleted. **NEXT: S19 — Crash BETTING + server-authoritative cash-out (security-grade: realtime-integrity + ledger-security). Bets during WAITING (single debit, distinct betId, optional autoCashout); manual cashout during RUNNING server-stamps the multiplier AT RECEIPT (≥C is loss); auto-cashout t wins iff t≤C; exactly one credit per winning bet, idempotent. PERSIST the round tuple (round_server_seed, round_id, round_number, C) at round START — reuse GameRound (MULTIPLAYER discriminator) + nullable per-round-seed col/JSONB, NOT a parallel crash table; migrate DEFAULT_CRASH_EDGE=0.01 into DB GameConfig. Then S20 recovery/latency-fairness.** Carry-forward from S14 still open for realtime/poker: add a cursor lost-update concurrency test pattern.
- 2026-06-28: **P2 NOW FULLY DEMOABLE.** Consolidated seed migration e5f6a7b8c9d0 (merged→a648a16, ledger-security PASS) seeds DB GameConfig+GameLimit for the 5 instant Originals (limbo, pocketdice, plinko, keno, roulette) — the stateful S12/S14 shipped their own seeds, so all 9 Originals are now API-playable. Integration tip **a648a16**; single alembic head e5f6a7b8c9d0. params for plinko/keno tables + roulette pockets sourced at apply-time from engine.registry.default_config (single source of truth, no drift, equality-tested). Roulette cap-trap honored: max_bet=10_000_000 max_win=1_000_000_000 (≥max_bet·99 so common green wins never cap); keno max_win=6e9 covers the 57551× headline; plinko 1.4e6 covers 13.99×; limbo/pocketdice cap only extreme-tail jackpots (documented product limit). Playability test (tests/test_seed_instant_originals.py) binds RED(GameDisabled)→GREEN: runs a real bet through place_bet per game with balanced ledger. User chose "seed migration THEN P3" at the P2 phase boundary. **NEXT: P3 Crash S18 (first REALTIME game — WS actor + Redis pub/sub + server-stamped cash-out; reuses engine/games/_curve.py from S10 verbatim). S18/S19/S20 are security-grade (realtime-integrity-reviewer): no double-credit, server-authoritative cash-out, deterministic recovery from committed seed. S18 establishes the realtime actor pattern — handle carefully like the first-stateful S12.** Recurring infra note: the shared .venv editable-installs point at MAIN, so workers/reviewers exercise worktree code via PYTHONPATH={worktree}/{engine,app,verifier}/src; and Postgres host port 5432 contends across worktrees (use a dedicated PG on an alt port via DATABASE_URL for DB tests). Worker mid-stream API stalls remain common on long agents — resume via SendMessage(agentId); nothing lost when the worktree is clean on stall.
- 2026-06-28: **S14 HiLo (SECOND stateful game) PASSED both reviewers, merged→f50a67f. ALL NINE ORIGINALS NOW DONE (S9 Dice, S10 Limbo, S11 PocketDice, S12 Mines, S13 Plinko, S14 HiLo, S15 Keno, S16 Roulette + the stub) — P2 game roster complete; only optional S17 extensions remain.** Integration tip **f50a67f**; registry loads all 9; single alembic head d4e5f6a7b8c9; merged-tree purity-empty/ruff/mypy(40)/lint-imports(2 kept)/104 pure tests green. S14 EXTENDED the S12 stateful saga for progressive-draw games (the design that S25 Blackjack/Baccarat inherits): rng-into-step (counter-indexed stream resumed per /action; seed never enters state), the {cursor,game} server_state envelope, and StatefulGame.public_view (the game's own client-safe projection at /bet-open + /state). **The StatefulGame protocol is now COMPLETE (init, step+rng, validate_input, public_view) — S25 should need no 5th method.** Two advisor consults drove the forks: (1) rng-into-step over pre-committed-sequence/seed-in-state; (2) public_view over init-returns-Outcome. Both reviews security-grade-thorough (ledger-security inspected a LIVE round to prove no seed in server_state + caught a simulated leaky public_view; engine-purity confirmed verifier path untouched + parity non-vacuous over the full step sequence). 3 non-blocking carry-forwards recorded in the S14 row (notably: add a HiLo-specific cursor lost-update concurrency test when revisiting realtime/poker). Worker stalled mid-stream twice again (API stream errors during long exploration) — resumed cleanly, nothing lost; fix-cycle 2 added public_view. **NEXT: P2 is demoable. Remaining before the P2 demo: ONE consolidated seed migration is NOT needed (each stateful game S12/S14 already shipped its own seed migration; the instant Originals S10/S11/S13/S15/S16 still need their GameConfig/GameLimit seeded — see their rows for values). Decide: either author the consolidated instant-Originals seed migration now, or proceed to P3 Crash (S18, reuses _curve.py from S10) / optional S17. Check the implementation-steps DAG + with the human on P2-demo vs P3 sequencing.**
- 2026-06-28: **S12 Mines (FIRST STATEFUL GAME) PASSED both reviewers, merged→23b00fe.** Integration tip now **23b00fe**; registry loads all 8 games; single alembic head c3d4e5f6a7b8; merged-tree purity-empty/ruff/mypy(39)/lint-imports(2 kept) clean; pure mines tests 71 passed. Established the server-held-state SAGA (see the S12 row for the full pattern S14/S25/Blackjack inherit): opaque-state seam, server-only game_rounds.server_state column + /state redaction, StatefulGame.validate_input protocol fence, SELECT FOR UPDATE per-action serialization, single-debit-open/single-credit-cashout. Both reviews were security-grade-thorough: ledger-security EMPIRICALLY proved the FOR UPDATE lock (removed it → real lost update) and the redaction (layout only in server_state), reconciliation Σ=0 over 840 entries, instant-path regression clean (116 passed); engine-purity confirmed EV=1-edge binding + parity non-vacuous + purity. Worker stalled mid-stream twice (API stream errors) but the resumed agent completed in 2 clean increments (engine 6ac9ecc → app saga 33fd1c3); nothing was lost (worktree was clean on the first stall). Worktree removed, branch deleted. **NEXT: S14 HiLo (§A.7) — serialized after S12 (REUSES this saga, must NOT re-build it). Carry the 3 S12 advisories into S14: re-issue stranded credit on /bet-replay+/state, write a settlement AuditEvent, the advisory-guard TOCTOU.** After S14: all Originals done except optional S17 → then the consolidated seed migration before the P2 demo. (S12 already ships its own seed in migration c3d4e5f6a7b8.)
- 2026-06-28: **INSTANT-ORIGINALS WAVE COMPLETE.** S15 (Keno) PASSED engine-purity review @ 99ccc05, merged→061ca8f (registry conflict resolved keeping all entries + the default_params import). All four instant Originals dispatched off dcba8ce are now merged: S11 PocketDice→f8d4184, S16 Roulette→997d178, S13 Plinko→ea7690d, S15 Keno→061ca8f. Integration tip **061ca8f**; registry loads all 7 games [dice, keno, limbo, plinko, pocketdice, roulette, stub.coinflip]; merged-tree purity-empty / ruff / mypy(38) / lint-imports(2 kept) all clean. CARRY-FORWARD landed: engine/src/engine/sampling.py sample_without_replacement (S15→reused by S12 Mines). All worktrees/branches for the wave removed. SEED VALUES for the consolidated pre-demo migration collected across S10–S16 (see each row). NEXT: STATEFUL Originals **S12 Mines** (§A.6, reuse sampling.py) + **S14 HiLo** (§A.7) — they need the StatefulGame init/step/post_action flow that does NOT yet exist; first stateful game establishes it (NOT a dice-template copy), wires /bet + /action via the shared S6 one-active-(user,game)-round guard + action serialization (reuse, don't re-implement). Must first AUDIT what S6 actually provides for the stateful /action path before dispatching. These touch app/ and are security-grade for the bet loop.
- 2026-06-28: S10 (Limbo) PASSED engine-purity review @ c9b45ea, merged→dcba8ce. Established `engine/games/_curve.py` crash_point carry-forward (verbatim→S18) + confirmed the Original-copy template (dice-shaped InstantGame + validate_input + rtp_heavy split). Cycle-2 was comment-only (ff9c5bc): corrected the _curve.py instant-bust comment so S18's bust-rate test uses the continuous P(f<edge)=edge, not the floored P(X==1.00)≈2·edge. Merged-tree verify (engine-pure, no DB): 31 passed/8 deselected + ruff/mypy(33)/lint-imports(2 kept)/purity-empty. S10 worktree removed, branch deleted. Integration tip dcba8ce. NEXT: fan out instant Originals {S11 PocketDice, S13 Plinko, S15 Keno, S16 Roulette} off dcba8ce — only shared file engine/registry.py (additive, serialized merge); no per-worker migrations. STATEFUL {S12 Mines, S14 HiLo} handled separately after (first stateful establishes the StatefulGame init/step/post_action flow).
> Append-only. One line per state change.
- 2026-06-27: Kit authored (plan + steps + orchestrator + this tracker). Status NOT STARTED, S1..S42 = pending. Next: paste the orchestrator prompt, run SETUP → S1.
- 2026-06-27: SETUP done — toolchain present (uv 0.9.17, pnpm 11.7, node 22, docker 29.5, Task 3.51; lefthook absent → pnpm devDep). Created integration branch `casino-games/main` off 0-commit master.
- 2026-06-27: S1 PASSED @ 7207cac (2 cycles). Worker scaffolded monorepo; reviewer flagged rtp-gate RED on empty collection; fix tolerates pytest exit-5. All gates (ruff/mypy/lint-imports/pytest/rtp-gate/compose) confirmed green independently. Next eligible: S2, S4, S5 (parallel).
- 2026-06-27: S2/S4/S5 ran in PARALLEL worktrees, all PASSED 1-cycle (S4 fairness-rng-review, S5 engine-purity-review, S2 general-review). Merged S4→S5→S2 into casino-games/main @ a539a79 (disjoint files, conflict-free). Full gate suite re-run on merged tree: ruff/mypy(11)/lint-imports(2 kept)/purity-grep-empty/20 tests green. Worktrees removed. Next eligible: S3 (ledger). NOTE: S8 (auth) deferred behind S3 — its welcome-grant must use ledger.grant() (single-owner rule), so S8 needs S3 despite DAG listing only S2.
- 2026-06-27: S3 PASSED @ 479f3de (2 cycles, ledger-security-review). Cycle1 code correct; reviewer flagged DB-idempotency fence untested → cycle2 added concurrent-same-key test (RED proven when constraint dropped) + multi-wallet conservation. Merged→6fa6c5d; full suite 28 tests green. SPINE main.py CONTENTION: S6/S7/S8 all register routes in app/main.py → SERIALIZE them. Path to S9 gate: S6→S7→S9 (S8 not a prereq for S9). Next: S6 (bet loop) solo.
- 2026-06-27: CI DEFECT (pre-existing, from S3) found while reviewing S7: the CI `python` job ran `pytest -q` with NO postgres service, while conftest needs a REAL alembic-migrated DB (SYSTEM+house seed from S2 migration, not create_all) → every DB test would ERROR → python job latently RED. Unnoticed because CI never executed (no remote) and gates were verified LOCALLY with Postgres up. S3–S6 were LOCALLY green, never CI-green. Fixed inside S7 (which owns ci.yml + adds DB tests): postgres:16 service + DATABASE_URL + `alembic upgrade head` before pytest. Proven via the exact recipe on a CLEAN/empty DB (down -v → up → migrate-from-base → 45 green).
- 2026-06-27: S6 PASSED @ 55add13 (1 cycle, ledger-security-review VERDICT: PASS). Bet loop saga verified server-authoritative: outcome from (server/client seed,nonce) via pure engine; single debit(WAGER)/credit(WIN) under DB UNIQUE(idempotency_key) + idempotent crash-replay re-credit; nonce FOR UPDATE monotonic; limits-before-debit, cap-after-multiplier; one AuditEvent w/ configVersion from DB. 4 advisory findings, none blocking, all deferred to proper scope (first-bet NonceCounter race→S7, RG-deny-untested→S38, active-seed-"latest"→S7, /state query-string user_id→S8) — threaded into S7/S8 notes. Merged→20eeb4d (--no-ff); full gate suite re-run on merged tree: ruff/mypy(22)/lint-imports(2 kept)/purity-grep-empty/35 tests green. Next: S7 (verifier + RTP harness + CI gate) solo on main.py.
- 2026-06-28: S9 REVIEW GATE → human GO (scale Originals) + chose "dedicated hardening step first". S9.5 (gate-inserted) PASSED @ 4146303 (ledger-security PASS): per-game validate_input seam + token-only identity on /games router. Merged→8f0c025; clean-DB verify 80 passed/4 deselected. ORCHESTRATION PLAN for P2 {S10..S17}: only shared file = engine/registry.py (additive REGISTRY dict entry → trivial serialized-merge resolution). NO per-worker alembic migration (would create sibling heads); per-game Verify is engine-pure using registry default GameConfig. Live GameConfig/GameLimit seeding for S10–S16 to be CONSOLIDATED into ONE migration before the P2 demo (workers report their edge/params + stake/max_win limits). INSTANT games: S10 Limbo(+_curve.py CF→S18), S11 PocketDice, S13 Plinko, S15 Keno, S16 Roulette — true dice-template copies (+validate_input). STATEFUL games: S12 Mines, S14 HiLo — different StatefulGame flow (init/step, post_action, server-held state); first stateful game establishes that flow → handle SEPARATELY/carefully, not as simple copies. Dispatching instant wave first.
- 2026-06-27: S8 + S9 ran in PARALLEL (disjoint files + infra: S8 auth/main.py/economy+DB on 5432; S9 engine/dice+registry, NO DB). Both PASSED 1st review. S8 @ 8178437 (ledger-security PASS) merged→9c6e61e. S9 @ 4cb169c (engine-purity PASS, gate-grade) needed cycle-2 (84574ae) to align RTP marking to rtp_heavy convention (was unmarked→would slow every PR + skip rtp-gate job; now fast 1e6 {50,98} PR + heavy 1e7 {2,50,98} wired into rtp-gate). Merged→884cf62. FINAL SPINE VERIFY on CLEAN DB (CI recipe): 3 migrations from empty + PR pytest 59 passed/4 deselected + rtp-gate `-m rtp_heavy` 4 passed (stub+3 dice) + ruff/mypy(31)/lint-imports(2 kept)/purity-empty. ⛔⛔ S9 REVIEW GATE REACHED — STOPPED, awaiting explicit human go before S10+. Open carry-forward for the gate: per-game input-range fence (target) at app/bet-loop boundary. P1 SPINE COMPLETE (S1-S9); integration tip 884cf62.
- 2026-06-27: S7 PASSED @ dc2717f (+CI fix 0f7316f) (1 cycle, fairness-rng-review VERDICT: PASS). Reviewer personally confirmed: verifier==server VERBATIM (identical engine path create_rng→play; no duplicated outcome math — only string-literal labels outside engine); commit-reveal secrecy (serverSeed null until ServerSeed.rotated_at set); RTP teeth (target=0.95 raises, 0.99 passes; CLT half-width, uncapped). Merged→b8a0fd0 (--no-ff); merged-tree re-verified via CI recipe on a CLEAN DB: 3 migrations from base + 45 passed/1 deselected, ruff/mypy(23)/lint-imports(2 kept)/purity-empty. S6/S7 worktrees+DB torn down. SPINE: S8 (auth) is the last main.py-contending spine step — now eligible (prereqs S2,S3 passed). Path to gate: S8 then S9 (Dice = ⛔ REVIEW GATE — STOP for human go after it passes).
- 2026-06-28: DOCKER RECOVERY + S33 dispatch. Docker Desktop wedged ~13h (disk hit ~97% during S30 ENOSPC → VM crashed; quit/reopen + 30m+60m watchers all failed while an ORPHANED com.apple.Virtualization.VirtualMachine pid lingered holding the wedged VM). FIX: user freed ~14GiB; killed the orphan VM pid; `open -a Docker` fresh boot → engine healthy. Containers were stale (created before reboot, NO host port binding) → `docker compose up -d --force-recreate postgres redis` rebound 0.0.0.0:5432/6379; both healthy; `alembic upgrade head` applied all 10 migrations to base (head b8c9d0e1f2a3). S32 PG durable test RE-CONFIRMED green (test_poker_ws.py 9 passed, was 8+1 PG-skip). Then dispatched S33 off tip a5766a2 → worker green (11 tests) → ledger-security-reviewer CHANGES_REQUESTED with a REPRODUCED money-printing exploit (seat-reuse BUYIN key collision; see S33 row) → fix cycle 1 in progress (worker a5a3f3ad4012063f7, bg). Infra now UP for the rest of P6 + P7.
